{
  "type": "postgres",
  "port": 5432,
  "host": "database_merge",
  "username": "docker",
  "password": "merge",
  "database": "merge_skill",
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "entities": [
    "./src/modules/*/entities/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}